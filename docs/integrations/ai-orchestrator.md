# AI Orchestrator

The AI Orchestrator is currently source-only and has no production Place connection. Future natural
language requests call versioned Place Tool contracts and use Place indexes through Place-owned use
cases. Direct database access, shared credentials, and unscoped writes are prohibited.
