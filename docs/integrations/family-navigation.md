# Family navigation

Place consumes `family-navigation.v1` through a frontend platform adapter. The workspace-level
composer owner is unresolved, so Stage 1 uses an empty `not-integrated` fixture and embeds no service
destinations. Integration is blocked until ownership, manifest publication, and failure fallback are
approved.
