# Errors and versioning

Errors expose stable machine codes, safe human messages, correlation references, and retry guidance.
They never include internal addresses, stack traces, provider payloads, selectors, profile paths, or
credentials. Breaking published-contract changes require a new major contract and bounded migration.
