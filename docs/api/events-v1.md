# Events v1

Publish an event only after a real external consumer is approved. Events use a versioned envelope,
opaque Place references, occurrence time, producer identity, and stable idempotency identity. Private
content, credential references, and raw provider captures are excluded.
