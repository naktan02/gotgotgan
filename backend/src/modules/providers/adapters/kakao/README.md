# Kakao adapter

Kakao 공식 검색과 향후 계정 import/outbound capability를 이 폴더가 소유한다. Stage 6에는 Local
keyword search의 문서화된 ID, rectangle, page cursor와 원문 링크만 사용한다. 다른 공급자
parser나 canonical Place 규칙을 import하지 않는다.
