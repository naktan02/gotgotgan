# Visits module

Visits owns immutable, repeatable visit occurrences. `visited`, first/last visit, and count are query
projections derived from those occurrences. Other modules neither write this schema nor duplicate
those facts.
