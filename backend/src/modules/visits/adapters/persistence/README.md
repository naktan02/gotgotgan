# PostgreSQL visits adapter

The runtime can append and read Visit occurrences but cannot update or delete them. Summary queries
derive visited state and repeat statistics from immutable occurrences.
